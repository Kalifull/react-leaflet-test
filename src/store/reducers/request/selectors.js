export const selectRequests = (state) => state.requestInfo.requests;

export const selectRequestError = (state) => state.requestInfo.error;

export const selectRequestLoadingStatus = (state) => state.requestInfo.loadingStatus;
